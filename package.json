{
  "name": "farmtory",
  "version": "0.1.0",
  "scripts": {
    "build": "npx esbuild src/App.tsx --bundle --minify --sourcemap --outfile=public/bundle.js",
    "dev": "npm run build -- --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.17.8",
    "phaser": "^3.55.2"
  }
}
